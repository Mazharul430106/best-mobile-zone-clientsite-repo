import React from 'react';
import useTitle from '../../Hooks/useTitle/useTitle';

const Register = () => {
    useTitle('Register');
    return (
       <div>
        
       </div>
    );
};

export default Register;